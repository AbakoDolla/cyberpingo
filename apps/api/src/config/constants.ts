export const API_PREFIX = '/api/v1';

export const SERVICE_NAME = 'cyberpingo-api';

export const SERVICE_VERSION = '0.1.0';

/** Name of the cookie holding the refresh token (HttpOnly, SameSite, Secure in production). */
export const REFRESH_TOKEN_COOKIE = 'cyberpingo_refresh_token';
