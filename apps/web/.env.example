# ---------------------------------------------------------------------------
# CyberPingo web - example environment file.
# Only VITE_* variables are exposed to the browser bundle.
# NEVER put DATABASE_URL, JWT secrets or any backend credential here.
# ---------------------------------------------------------------------------

VITE_API_URL=http://localhost:3000/api/v1
