import { PagePlaceholder } from '@/components/layout/PagePlaceholder';

const LeaderboardPage = (): JSX.Element => (
  <PagePlaceholder
    title="Leaderboard"
    description="Ranking computed by the backend from XP earned on the platform."
  />
);

export default LeaderboardPage;
