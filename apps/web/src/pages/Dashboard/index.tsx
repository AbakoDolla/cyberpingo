import { PagePlaceholder } from '@/components/layout/PagePlaceholder';

const DashboardPage = (): JSX.Element => (
  <PagePlaceholder
    title="Dashboard"
    description="Your learning overview: current course, streak, XP and next lesson."
  />
);

export default DashboardPage;
