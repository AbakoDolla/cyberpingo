import { PagePlaceholder } from '@/components/layout/PagePlaceholder';

const CoursesPage = (): JSX.Element => (
  <PagePlaceholder
    title="Courses"
    description="Catalogue of CyberPingo learning paths, served by GET /api/v1/courses."
  />
);

export default CoursesPage;
